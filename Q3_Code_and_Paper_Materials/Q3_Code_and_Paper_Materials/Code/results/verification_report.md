# 问题3第二问：按题面条件的粗网格复算

日期：2026-09-25。初温和环境温度均为 −30 ℃，`gammaIce=3.5`，`kFreeze=0.4 s^-1`。空间网格 `[2 3 4 4 2]`，五片加热功率密度各不超过 1 W/cm²，面积各为 25 cm²。电荷上限 20 C/cm²、电流上限 0.5 A/cm²、单片最低电压 0.30 V、最大局部冰体积分数 0.99。冰参数为试用值，尚未重新标定。

纯预热阶段电流严格为零，仿真在设定的停热时刻结束，并检查五片平均温度是否均超过 0 ℃以及其他启动约束。题目没有规定其后的电流曲线，本次不再模拟停热后的加载，也不设置额外的持续运行时长。协同启动从零时刻同时加热和加载，电流密度为 `min(0.005 t,0.3) A/cm²`。

| 模式 | 功率密度 q（W/cm²） | 设定加热时长 | 实际加热时长 | 各片辅助能耗（J） | 总能耗（J） | 题面启动时刻 | 最大局部冰体积分数 | 结果 |
| --- | --- | ---: | ---: | --- | ---: | ---: | ---: | --- |
| 纯预热 | `[1,1,0.9,1,1]` | 35 s | 35 s | `[875,875,787.5,875,875]` | 4287.5 | 35 s | 0.011757 | 启动达标：五片均超过 0 ℃ |
| 恒功率协同 | `[1,1,1,1,1]` | 60 s | 30.2438 s | `[756.094,756.094,756.094,756.094,756.094]` | 3780.471 | 30.2438 s | 0.119705 | 成功 |

纯预热停热时五片平均温度分别约 `[0.236,13.490,17.514,13.490,0.237] ℃`，累计电荷为零，全程最低单片电压约 1.2256 V。协同启动达标时五片平均温度分别约 `[0.010,14.484,19.255,14.492,0.048] ℃`，累计电荷约 2.2867 C/cm²，全程最低单片电压约 0.6308 V。协同模式最严重冰积累发生在端部；全程最大局部冰体积分数约 0.1197，高于纯预热方案的 0.01176。

当前采用的纯预热启动判定是停热时五片均超过 0 ℃，同时满足模型中的电压和冰约束。因此 `result.success=true` 对应纯预热策略启动达标，35 s 就是该策略在这组候选参数下的启动时间，不再另计停热后的加载时间。

无辅助热源、使用同一规定电流曲线的自冷启动在约 61.907 s 因单片电压先达到 0.30 V 而失败，届时累计电荷约 9.572 C/cm²、最大局部冰体积分数约 0.3808。

上述两组辅助方案是此前有限预算搜索得到的候选，本次按修正后的题面模型复算，**不能称为已证明的全局最小能耗**。此前每组分别搜索 12 次和 10 次。协同候选的请求加热时长是 60 s；由于首次达标即关闭加热并停止仿真，实际加热时长和能耗按 30.2438 s 计算。若论文把 `t_h` 定义为预先固定、不得在达标时关闭的持续时间，则必须把决策变量重新设为接近 30.2438 s 并重新验证。

`test_q3_optimization_contract` 和 `test_q3_auxiliary_heating` 在修改后通过；两组候选均已用相同粗网格复算。没有做细网格复核，符合本轮比较范围。
