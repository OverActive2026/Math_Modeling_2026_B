# 复现工具

MATLAB R2023b环境下，先进入本目录。

```matlab
export_saved_trajectories
```

此命令已运行成功，只读取已保存MAT，不重新仿真。输出两项最佳方案及全满功率协同基准的CSV、PNG和矢量PDF。

可选独立复算：

```matlab
reproduce_selected
```

该命令会重新求解三个工况，耗时取决于机器；结果写入新建“独立复算”目录。本次整理没有执行该重算入口，因此不声称它已通过实际运行验证。物理入口会使用本包Code的优先路径。Code已包含原工程历史档案；本工具直接使用随包保存的三个输入和求解选项复算。

原表4及原工程文件没有被本工具改写。导出曲线每行对应保存输出，真实数值来自MAT而非重新拟合或手绘。
